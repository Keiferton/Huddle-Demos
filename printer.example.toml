[printer]
# Replace with the device reported by list-ports; no port is auto-selected.
port = "/dev/ttyACM0"
# Verify this against your printer's documentation.
baud = 115200
timeout = 5.0
startup_wait = 2.0
